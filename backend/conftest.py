import pytest


@pytest.fixture(autouse=True)
def never_message_real_people(settings):
    """Тесты не отправляют сообщений в WhatsApp ни при какой конфигурации.

    Отдельные тесты выставляют этот флаг сами, но полагаться на это
    нельзя: заказ отправляет поставщику сообщение через обработчик, а
    в .env лежат боевые учётные данные. Одна забытая переменная — и
    прогон набора пишет живым людям.
    """
    settings.MOCK_GREEN_API = True
